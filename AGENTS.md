# iRC project note

When changing iRC behavior or preparing a release, check interoperability with older released clients still used in the guild. Explicitly warn the user before release if the update could cause mixed-version issues, especially with addon-message formats, ruleset checksums or authority, presence replies, Guild Found verification, and guild sync. State which older versions are affected and what users would observe. Do not assume everyone updates at the same time.
